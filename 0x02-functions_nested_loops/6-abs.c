#include "main.h"
/**
 * _abs - Computes absolute value of integer
 * @n: integer to be computed
 *
 * Return: Absolute value of integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
